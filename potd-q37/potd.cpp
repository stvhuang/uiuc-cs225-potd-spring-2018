#include "potd.h"
#include <vector>
using namespace std;

vector<int> evaluate() {
    vector<int> ret(7);
    ret[0] = 0;  // change to 0 or 1
    ret[1] = 1;  // change to 0 or 1
    ret[2] = 1;  // change to 0 or 1
    ret[3] = 0;  // change to 0 or 1
    ret[4] = 0;  // change to 0 or 1
    ret[5] = 1;  // change to 0 or 1
    ret[6] = 1;  // change to 0 or 1
    return ret;
}
